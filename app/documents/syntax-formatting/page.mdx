# ข้อกำหนดทางไวยากรณ์
ในการ import เอกสารจำเป็นต้องมีการกำหนดตัวแปรให้กับข้อความที่จะแทนที่

Injecting value into placeholder composes into 2 sections.

## Structure of Model
the model, now we called Placeholder, consists of 2 parts. `{{}}` and variable inside `{{variable}}`

- `Placeholder` is the scope for declaring the variable.
- `Variable` is the thing you declare to inject the value later
- `__init__` is the first letter of the variable before _ was tagged.
- `entity` is the group of relation of data. We weill use entity to group data into

## Placeholder

## Variable

## Entity definition
To make sure that the engine is able to expose value into variable, you may required to entitle the entity for the variable.